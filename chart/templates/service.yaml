apiVersion: v1
kind: Service
metadata:
  name: {{ include "airdrop-relayer.fullname" . }}
  labels:
    {{- include "airdrop-relayer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "airdrop-relayer.selectorLabels" . | nindent 4 }}
